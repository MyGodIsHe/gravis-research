using SFB;

namespace Constants
{
    public static class SerializationHelper
    {
        public static readonly ExtensionFilter Extension 
            = new ExtensionFilter("Graph files", SerializationConstants.FileExtension);
    }
}